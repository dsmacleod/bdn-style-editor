name = "bdn-style-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

rules = [
  { type = "Text", globs = ["**/*.html"] }
]

# The CLAUDE_API_KEY secret is added via:
#   wrangler secret put CLAUDE_API_KEY
